this is a prior conversation with an agent:

user: "I am designing a proof of concept project for a hackathon track: "design something that improves the developer experience, reagardless of whether it is something that is usable on Warp or not. You do NOT have to build this with Warp." Inspired by this blog regarding taxonomy of silent agent failures, which applies especially to coding agents, I am interested in designing a MCP proxy tool that intercepts agent tool calls or reads the file states of a codebase to determine what an agent is doing and when it might be failing: https://www.uselemma.ai/blog/a-taxonomy-of-agent-failures; I want to see which of these might be 1) applicable to the context of coding agents, 2) feasible to implement and get demo videos out of with a fixed or small set of cases that I can use to demonstrate this conceptually (I plan to use a mix of treesitter, AST, exact search, LSP, and vector embedding pipelines to give a heuristic score), out of a score of 10 for each item above. The goal is to "make this generalizable to the hacker experience": "we realized tha everyone has a different workflow, some might use 10 subagents with superpowers and git worktrees, some might like using a single claude fable 5 session or some might lean super heavily into skills, so we decided to take a step back, talk to each other, and identify the common problems that we all found in all of our workflows"; and the lack of real human in the loop and being hard to verify code is one of the problems we found."
agent: 

I think this is a **very strong hackathon framing**, with one important adjustment: do not position the project as “detecting whether an agent is failing” in the general case. Position it as a **runtime linter / flight recorder for coding agents** that detects *evidence of silent failure* by comparing: intent → actions → repository state → agent claims** That is almost exactly the relational insight behind Lemma's taxonomy: the seven failures are not properties of individual actions; the([Model Context Protocol][1])vents, established facts, or what the user understood. Here, **Applicability** means “how often this meaningfully maps onto coding agents,” while **POC feasibility** means “how convincingly you could implement and demo a detector for a small but non-cheating set of cases.| Failure mode | Coding applicability | POC feasibility | My recommendation  |------------------------- | -------------------: | --------------: | --------------------------- | | **Skipped Work**          |            **10/10** |        **7/10** | Core feature                || **Out of Scope Work**     |             **9/10** |        **8/10** | Core feature                || **Instruction Violation** |            **10/10** |        **8/10** | Core feature                || **Integration Failure**   |             **9/10** |       **10/10** | Must build                  || **Retry Loop**            |             **8/10** |       **10/10** | Must build                  || **Hallucination**         |             **9/10** |        **7/10** | Narrow to verifiable claims || **Communication Failure** |             **6/10** |        **3/10** | Defer                       |The taxonomy's seven categories are Skipped Work, Out of Scope Work, Instruction Violation, Integration Failure, Retry Loop, Hallucination, and Communication Failure. Skipped Work **Applicability: 10/10** **POC feasibility: 7/10**;This is probably the **fundamental coding-agent failure**.User:Rename `UserManager` to `AccountManager` and update all references and tests.Agent:* renames the class;* updates three references;* misses two dynamic imports;* says “Done.";Nothing crashed. The diff looks plausible. This is exactly Lemma's definition: an obligation was opened but never closed. ([Lemma][2]) signals for your stack; You can combine:```text - Prompt obligation        ↓"rename UserManager everywhere"        ↓exact search ─── remaining UserManager referencesTree-sitter ──── remaining identifier nodesLSP ──────────── unresolved/remaining referencesAST diff ─────── expected symbol changed?tests ────────── failures related to old symbol```The hard part is:> How do you derive the expected obligation from arbitrary natural language?You will not solve that generally without semantic interpretation. But that is completely acceptable for the POC.Your demos can target **structurally verifiable obligations**:* Rename X everywhere.* Add parameter X to all implementations.* Remove usage of deprecated API X.* Update every subclass of interface X.* Implement method X.* Add tests for new behavior.* Change configuration value X.* Update references from A to B.You could maintain something like:```typescriptinterface Obligation {  description: string;  targetSymbols?: string[];  targetPaths?: string[];  expectedConditions: Predicate[];  evidence: Evidence[];  confidence: number;}```Your real contribution is then not perfect extraction. It is showing that:> **Coding-agent tasks can remain “open” after the agent has implicitly treated them as complete.**That is a compelling hackathon demo.---# 2. Out of Scope Work**Applicability: 9/10****POC feasibility: 8/10**Also extremely strong.Examples:> Fix this login bug.Agent additionally:* refactors unrelated authentication infrastructure;* modifies 17 files;* updates dependencies;* rewrites tests;* changes a public API.This maps directly to Lemma's “an action ran that no obligation called for.” ([Lemma][2])nusually good opportunity here because **code gives you structural information that general-purpose agents do not have**.For every changed file or symbol:```textHow far is this change from the task?```Your heuristic could combine:```textscope_score =    task_embedding_similarity  + dependency_graph_proximity  + symbol_reference_proximity  + explicit_path_match  + stack_trace_proximity  - unrelated_subsystem_penalty```For example:```textTask: "Fix null crash in avatar rendering"src/avatar/render.ts       0.96src/avatar/types.ts        0.82tests/avatar.test.ts       0.79src/auth/session.ts        0.13  ← suspiciouspackage.json               0.08  ← suspicious```Then display:> ⚠️ Agent modified `src/auth/session.ts`, but no dependency path, symbol reference, task phrase, or failing-test relationship connects it to the requested change.This makes an **excellent visual demo**.The important nuance is not to claim:> “This change is definitely out of scope.”Instead:> “This change has weak observable justification.”That is much more defensible.---# 3. Instruction Violation**Applicability: 10/10****POC feasibility: 8/10**I would make this one of your headline demos.Coding tasks frequently contain explicit negative constraints:> Do not modify the public API.> Do not change the tests.> Do not add dependencies.> Only modify files under `/frontend`.> Keep this backwards compatible.> Don't rename the existing methods.These are **much easier to verify than arbitrary task completion**.Examples:### “Don't modify tests”```textgit diff --name-only        ↓tests/auth.test.ts modified        ↓VIOLATION```### “Don't add dependencies”```textpackage.json AST before/after        ↓dependencies changed        ↓VIOLATION```### “Don't change the public interface”```textAST/public symbol graph before        ↓AST/public symbol graph after        ↓exported function signature changed        ↓VIOLATION```### “Don't touch authentication”```textchanged files    +dependency graph    +semantic classification        ↓auth subsystem modification detected```This is where Tree-sitter and LSP become genuinely useful rather than just impressive technology names.I would actually split your internal model into:```textOBLIGATIONSthings the agent must eventually make trueCONSTRAINTSthings that must remain true throughout the run```That mirrors Lemma's distinction between obligations and constraints. ([Lemma][2])your product an understandable mental model.---# 4. Integration Failure**Applicability: 9/10****POC feasibility: 10/10**This is your easiest slam dunk.Lemma defines this as essentially:> an error happened, but there is no evidence that the agent dealt with it. ([Lemma][2])example:```textAgent runs testsFAIL: 3 testsAgent edits unrelated fileAgent says:"Implementation complete. All tests pass."```Your system has extremely strong evidence:```texttest execution    ↓exit code 1    ↓failure signature ABCsubsequent actions    ↓no successful test executionfinal response    ↓"All tests pass"🚨 UNHANDLED FAILURE```Even without observing the final response:```textError introduced at t=32No corresponding successful recovery by t=91Agent stopped working```You can flag:> **Unresolved integration failure**A generalized state machine could be:```textOPEN FAILURE    test failure    compiler error    LSP diagnostic    failed tool call        ↓RESOLVED    corresponding successful retry    diagnostic disappeared    error explicitly acknowledged        ORABANDONED    run ends    unrelated work continues    success is claimed```This is probably your **best first implementation**.---# 5. Retry Loop**Applicability: 8/10****POC feasibility: 10/10**Extremely easy to detect and extremely easy to demonstrate.Lemma specifically describes repeated attempts at effectively the same action despite receiving the same failure. ([Lemma][2])ions:```textnpm testnpm testnpm test```Or slightly more subtly:```textpytest tests/foo.pypytest ./tests/foo.pypython -m pytest tests/foo.py```Then fingerprint the resulting failures.Something like:```textaction_fingerprint =    normalized_tool  + normalized_command  + normalized_targetsfailure_fingerprint =    error_type  + normalized_stack_trace  + relevant_output_hash```Then:```textsame action+same failure+no meaningful intervening state change+N repetitions=retry-loop confidence ↑```That last condition is powerful:> Repeating a test after changing relevant code is legitimate.> Repeating exactly the same test without changing anything relevant is suspicious.Your source observer makes this substantially smarter than ordinary tracing.This could produce a great demo timeline:```text12:01  npm test       ❌ SAME_FAILURE_9112:02  npm test       ❌ SAME_FAILURE_9112:03  npm test       ❌ SAME_FAILURE_91No relevant filesystem changes occurred between attempts.⚠️ Probable retry loop: 96%```---# 6. Hallucination**Applicability: 9/10****POC feasibility: 7/10**I would **not** attempt generic hallucination detection.Instead define a coding-specific subset:# Verifiable Claim MismatchThe agent says:> “All tests pass.”But the latest test execution failed.Agent says:> “I updated all callers.”Exact search finds three old callsites.Agent says:> “The project already has a `UserService` abstraction.”No such symbol exists.Agent says:> “I added validation to the API endpoint.”AST diff shows the endpoint was untouched.Agent says:> “This preserves backwards compatibility.”Public API diff shows a breaking signature change.Lemma defines hallucination as an assertion unsupported by what has actually been established. Coding environments make a useful subset of those assertions mechanically checkable. ([Lemma][2]) claims separately:```textAgent claim    ↓classify into verifiable predicate"tests pass"    ↓latest_test_result == PASS"updated all references"    ↓remaining_old_references == 0"implemented Foo"    ↓symbol_exists(Foo)"no API changes"    ↓public_api_diff == empty```Again, extracting claims from arbitrary prose is difficult.But for a hackathon you could even start with an exact/regex-driven claim detector:```regex/all tests pass/i/updated all (references|usages|callers)/i/no (breaking|API) changes/i/implemented .+/i```Then make verification sophisticated.That asymmetry is perfectly fine.---# 7. Communication Failure**Applicability: 6/10****POC feasibility: 3/10**I would leave this out of the initial POC.Lemma's definition depends on a strange reference point:> the work was successfully completed, but the user came away believing otherwise. ([Lemma][2]), embedding, and filesystem systems are not going to tell you what the human understood.You could weakly detect things like:```textAgent made significant repository changesFinal response contains:- no files mentioned- no outcome stated- no test status- no explanation of completion```But that is mostly a **response-quality linter**, not genuine communication-failure detection.The strongest signal would be the next user message:> You didn't update the tests.Then your monitor determines:```textActually, the tests were updated.```That would fit Lemma's definition well, but it requires observing subsequent conversation turns and interpreting the user's belief. Lemma itself notes the strongest signal can be contradictory follow-up behavior, although even that requires interpretation. ([Lemma][2])esearch direction; poor hackathon core.---# What I would actually buildI would narrow the product to **five detector families**.```text                   ┌─────────────────┐                   │   USER INTENT   │                   │                 │                   │ obligations     │                   │ constraints     │                   └────────┬────────┘                            │                            ▼┌──────────────┐    ┌─────────────────┐    ┌───────────────┐│ ACTION STREAM│───▶│ FAILURE MONITOR │◀───│  CODE STATE   ││              │    │                 │    │               ││ MCP calls    │    │ skipped work    │    │ Git diff      ││ shell calls  │    │ scope drift     │    │ AST           ││ tool results │    │ violations      │    │ Tree-sitter   ││ errors       │    │ retry loops     │    │ LSP           │└──────────────┘    │ failed recovery │    │ Tests         │                    │ claim mismatch  │    │ Search        │                    └────────┬────────┘    └───────────────┘                             │                             ▼                    ┌─────────────────┐                    │ EVIDENCE REPORT │                    │                 │                    │ confidence: .91 │                    │ evidence: [...] │                    │ timeline: [...] │                    └─────────────────┘```The five actual things I would ship are:### **1. Unresolved Failure**> A compiler/test/tool error occurred and was never observably resolved.Maps to **Integration Failure**.### **2. Repeated Failure**> The agent repeated substantially the same action, receiving substantially the same failure, without a relevant state change.Maps to **Retry Loop**.### **3. Constraint Breach**> A repository invariant explicitly requested by the user became false.Maps to **Instruction Violation**.### **4. Scope Drift**> The agent changed code with little observable relationship to its stated task.Maps to **Out of Scope Work**.### **5. Unmet Obligation / False Completion**> The run appears complete, but observable evidence contradicts an expected outcome or completion claim.Combines the most demoable parts of **Skipped Work** and **Hallucination**.That is already a very credible POC.---# The MCP part needs careful positioningAn MCP proxy is useful, but I would avoid making **MCP proxy** the fundamental abstraction.MCP hosts create clients that communicate with MCP servers, and tool invocation travels through those client-server connections. A proxy can therefore sit between an MCP client and downstream MCP servers and inspect those calls. But, by inference from that architecture, it has no inherent visibility into a host application's proprietary shell/editor/filesystem tools that never traverse that MCP connection. ([Model Context Protocol][1])fore architect:```text                 Coding Agent                      │            ┌─────────┴────────┐            │                  │       MCP actions        native actions            │                  │            ▼                  │      ┌───────────┐            │      │ MCP Proxy │            │      └─────┬─────┘            │            │                  │            └─────────┬────────┘                      ▼              Repository Observer              ───────────────────              Git state              Filesystem              AST              LSP              Tests```For MCP specifically, both local stdio connections and Streamable HTTP are standardized transports, so proxying the protocol layer itself is technically reasonable for MCP-visible actions. ([Model Context Protocol Blog][3])eralizable product idea** should be:> **We don't need your agent's internal reasoning. We observe the contract between intent, actions, and code state.**That makes the idea applicable to:* one giant Claude/Codex session;* ten subagents;* worktrees;* MCP;* skills;* custom agents;* IDE agents;* terminal agents.You are measuring the **shared external artifacts**, not requiring everyone to adopt the same orchestration pattern.---# Your strongest hackathon demo sequenceI would build five tiny repositories where the failure is deterministic.### Demo 1 — “You said you were done”```textTASKRename UserService to AccountService everywhere.AGENT RESULTMisses one reference.DETECTOR⚠️ Unmet obligationEvidence:src/jobs/cleanup.ts:41 still references UserServiceLSP references remaining: 1```**Skipped Work.**---### Demo 2 — “You weren't supposed to touch that”```textTASKFix the parser.Do not modify the public API.AGENT RESULTChanges exported parse() signature.DETECTOR🚨 Constraint violationExpected:parse(input: string): ASTObserved:parse(input: string, config: Config): AST```**Instruction Violation.**---### Demo 3 — “Why are you changing authentication?”```textTASKFix avatar rendering.AGENT RESULTTouches auth/session.ts.DETECTOR⚠️ Possible scope driftTask similarity: 0.08Dependency distance: ∞Symbol overlap: 0Failing-test relationship: none```**Out of Scope Work.**---### Demo 4 — “The agent is stuck”```textnpm test → same errornpm test → same errornpm test → same error```No relevant code change.**Retry Loop.**---### Demo 5 — Your killer demo```textnpm test❌ 3 failuresAgent:"Everything is implemented and all tests pass."```Then:```text🚨 Completion claim contradicted by repository evidenceLatest test run: FAILEDFailures unresolved: 3Successful test runs since failure: 0```This simultaneously demonstrates:* Integration Failure;* Hallucination;* false completion;* why ordinary traces showing “agent completed successfully” are insufficient.That is probably the **30-second demo people remember**.---## My overall ranking for the hackathon```text                         Demo value     DifficultyIntegration Failure      ██████████     ██Retry Loop                █████████      ██Instruction Violation     ██████████     ████Skipped Work              ██████████     ██████Out of Scope Work         █████████      █████Hallucination*            ██████████     █████Communication Failure     ████           █████████````*` specifically **verifiable coding claims**, not generic hallucinations.The project thesis I would pitch is:> **Coding agents fail silently because “the tool call succeeded” is not the same as “the task succeeded.” We build an agent-agnostic verification layer that continuously compares what the developer asked for, what the agent actually did, and what became true in the codebase.**That connects your group's observation—**every hacker has a different agent workflow, but everyone struggles to supervise and verify autonomous coding work**—directly to Lemma's core argument that successful execution signals do not establish semantic success. ([Lemma][2])tly, it gives you a project that feels much larger than “an MCP logging proxy”: **an observability and verification layer for agentic software development**, with MCP as one ingestion mechanism rather than the whole product.[1]: https://modelcontextprotocol.io/docs/learn/architecture?utm_source=chatgpt.com "Architecture overview"[2]: https://www.uselemma.ai/blog/a-taxonomy-of-agent-failures "A Taxonomy of Agent Failures | Lemma Blog"[3]: https://blog.modelcontextprotocol.io/posts/2025-12-19-mcp-transport-future/?utm_source=chatgpt.com "Exploring the Future of MCP Transports"
